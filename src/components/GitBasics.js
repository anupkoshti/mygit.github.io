

const GitBasics = () => {
  return (
    <div>
      <ol>
        <li>git init: will create a nw local GIT repository</li>
        <li>git clone : is used to copy a repository</li>
        <li>git add : add a file as it looks now to our next commit (stage)</li>
        <li>
          git commit : will create a snapshot of the changes and save it to the
          git directory
        </li>
        <li>git push : push local changes to the original</li>
      </ol>
    </div>
  );
};

export default GitBasics;
